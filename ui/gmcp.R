tabPanel(
  title = "Sequential Graphs Update",
  icon = icon("hourglass-half"),
  fluidRow(
    column(
      width = 10, offset = 1,
      fluidRow(
        column(
          width = 12,
          span("Sequential Graphs", style = "font-size:1.5rem;")
        )
      ),
      hr(),
      headingPanel(
        "Inputs",
        p("WIP")
      )
    )
  )
)
